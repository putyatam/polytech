#pragma once
#include <iostream>
#include <vector>
#include <random>
#include <string>
#include <vector>

using namespace std;
static int max_value = 50;
static int max_rank = 10;

bool isN(const std::string&);
void clearInputBuffer();
