begin
		int vec[6] := {-1, 2, 3, -4, -5, -6};
		sorted(vec);
   	for (int i := 0; i < len(vec); i := i + 1) do
				if (vec[i] < 0) then
						temp := -vec[i];
						write(temp);
				else
						temp := vec[i] * 10;
						write(temp)
				fi
		od
end